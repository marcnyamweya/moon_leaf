import 'package:flutter/material.dart';

class AppColors {
  static const Color coverPlaceholderColor = Color(0x1F888888);
  
  static Color filterColor(bool isDark) {
    return const Color(0xFFFFC107); // Same for both light and dark
  }
}